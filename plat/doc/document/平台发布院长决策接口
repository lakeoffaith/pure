1:发布科室-时间-挂号的接口
	测试医院为 四川大学华西口腔医院  id为104   科室：id=11092 口腔科
	step1:获得某个医院，某月的预约挂号收入；
	select trunc(p_date),sum(score),count(id) from orderitem where to_char(p_date,'yyyy-mm')='2016-03' group by trunc(p_date);

